# wspr configuration

[hotkey]
combo = "super+space"

[model]
size = "small.en"     	# tiny.en, base.en, small.en, medium, large-v3
device = "cpu"        	# cpu, cuda
compute_type = "int8" 	# int8 (CPU), float16 (GPU)
